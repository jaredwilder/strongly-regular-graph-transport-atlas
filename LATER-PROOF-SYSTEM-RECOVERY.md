# Later SRG proof-system / Conway-99 recovery lane

A later SRG research layer remains buried under `jaredwilder/erdos-close-campaigns/srg-conway99/`. This repository is the correct canonical home; **do not create a second SRG repository** for that material.

## What the later layer adds

The buried program goes beyond the original 119-row transport atlas. Its public reports describe a proof-producing SRG vertical slice with:

- typed SRG parameter/specification handling;
- deterministic semantic indexing over Mathlib concepts;
- proof search with non-conflated `PROVED`, `REFUTED`, and `STILL_OPEN` states;
- Lean source emission / compiler boundary;
- certificate-aware result import;
- a compiled complete-graph SRG route;
- an independently checked parameter refutation;
- Conway's `(99,14,1,2)` target retained explicitly as `STILL_OPEN`.

Public generated artifacts in the buried source include:

- `generated/srg_5_4_3_0.lean`;
- `generated/srg_5_2_0_1.lean`;
- result JSON objects for proved/refuted/open examples;
- `generated/conway_99_open_goal.lean`;
- `generated/srg-results.jsonl`;
- a 13 MB Mathlib semantic index.

The same buried program also records a widened conference/SRG transport pass through order 1300. The report records 118 decided overlap cases with zero disagreements, then structural completion of 71 `OPEN / OPEN` proposition equivalences; this widened pass did **not** merge the target SRG component with the Mathlib kernel component.

## Authority boundary

The original delivery stack under `srg-conway99/delivery/graph_bridge_real_delivery/` is already recovered byte-for-byte here under `atlas/`; it is **not** new debt.

The later reusable engine files named in the buried report (`srg_spec.py`, `srg_semantic_index.py`, `srg_proof_search.py`, `srg_lean_bridge.py`, `srg_result_import.py`) have not yet been independently recovered into this repository. The later generated artifacts and reports therefore establish a real follow-on program, but not yet a complete portable source package.

## Recovery target

Recover the later engine bytes and then migrate the later layer into a dedicated subdirectory here, preserving original filenames and hashes. Until then, the canonical provenance pointer is:

`jaredwilder/erdos-close-campaigns/srg-conway99/`

**Routing verdict:** existing top-level home confirmed; later proof-system layer = promotion/recovery debt into this repository, not a new repository.
